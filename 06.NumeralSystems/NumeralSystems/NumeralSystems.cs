using System;

namespace NumeralSystems
{
    class NumeralSystems
    {
        static void Main(string[] args)
        {
            /*Console.Write("Enter a number to convert from decimal to binary numeral system: ");
            int number = int.Parse(Console.ReadLine());
            int[] array = new int[8];
            int i;

            for (i = 0; number > 0; i++)
            {
                array[i] = number % 2;
                number /= 2;
            }     
            
            for (i = i - 1 ; i >= 0; i--)
            {
                Console.Write(array[i] + " ");
            }*/

           /* Console.Write("Enter a binary number to convert to decimal: ");
            int number = int.Parse(Console.ReadLine());
            double[] array = new double[number];
            double result;
            double sum = 0;

            for(int i = 0; number > 0; i ++)
            {
                result = number % 10;
                array[i] = result * (Math.Pow(2, i));
                sum += array[i];
                number /= 10;
            }         
            Console.Write(sum);*/
        }
    }
}
